/*WAP to left rotate a dynamica 1D array with size 'n'(use calloc)*/
#include <stdio.h>
#include <stdlib.h>
int main()
{
    int i, n,*ptr;
    scanf("%d", &n);

    ptr = (int *)calloc(n, sizeof(int));

}